Procedure dot_product(v1,v2:integer array);
var ps: integer;
var i,j : integer:
Begin
read v1,v2;
ps= 0;
for i = from 0 to v1.length do 
   for j =from O to v1.length do
   ps =(v1[i] * v2[j])
end_for
end_for
end
