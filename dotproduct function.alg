function dot product(v1(arr),v2(arr))
var i = 0;
var ps = 0;
for(i=0, i<v1.length;i++){
ps += v1[i] * v2[i];
return ps;
}

var i = 0;
for(i=0; i<v1.length ; i++);
 for (i=0; i<v2.length ; i++);
